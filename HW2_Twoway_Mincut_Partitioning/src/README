--How to Compile
In this directory, enter the following command:
$ make
It will generate the excutable file
"main" in the same directory.
If you want to remove it please enter the following command:
$ make clean
If you want to remove the excutable file and the output file enter the following command:
$ make veryclean

--How to Run
Usage: ./<executable file> -c <cells_file_name> -n <nets_file_name> -o <output_file_name>
e.g.:
./main -c ../testcases/p2-3.cells -n ../testcases/p2-3.nets -o p2-3.out

Parameters:
    -c	: Attach cells data file name
    -n	: Attach nets data file name
    -o	: Attach output data file name
    -d	: Show detail message [Option]
    -t	: Show time message [Option]
    -h	: Show the command usage [Option]
    -s  : Engage pin number sort for initial partition [Option]
